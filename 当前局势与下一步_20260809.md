# InternViT 云端教师：当前局势与下一步

> 临时执行文档，放在仓库根目录便于本轮实验使用。实验稳定后再整理进 `docs/`。

## 最终目标

目标不是证明“大模型分类准确率高”，而是证明以下完整链路：

```text
边缘检测到持续漂移
  -> 上传代表性样本
  -> 云端冻结大型视觉模型产生 task logits / visual features
  -> 云端只更新 MV-ViT-S 的 AdaptFormer
  -> 下发约 1.2 MB 增量参数
  -> 边缘漂移性能恢复，clean 能力基本保持
```

决定性结论必须来自同数据、同漂移、同 Adapter 配置下的严格对照。真实 u=1 的主判据是：
无上传标签的 cloud-guided Adapter 显著超过未适配 Edge，并接近有标签 label-only 上界；
“在已有 CE 上继续叠加 teacher 是否额外增益”只作为 hybrid 消融。Qwen3-VL condition 已是
归档消融，不回到该路线。

## 已有证据

完整 train split 的 InternViT-6B 特征已提取，冻结 backbone 上训练的 linear head 得到：

| 模型 | Clean | Illumination 1.0 | Blur 0.8 | Noise 0.6 | 漂移平均 |
|---|---:|---:|---:|---:|---:|
| Edge baseline | 93.84% | 81.82% | 84.59% | 79.51% | 81.97% |
| InternViT-6B + mixed-drift linear head | 93.37% | 91.06% | 89.06% | 73.19% | 84.44% |

因此 InternViT 已通过 illumination、motion blur 的教师准入，但 sensor noise 未通过。
不能用平均值掩盖 noise，也不能直接开始三漂移蒸馏。

### Noise head gate 实际结果（2026-08-09）

复用已有特征缓存完成四种 head 对照后，`noise_weighted_mlp` 被自动选中：

| Head | Clean | Illumination 1.0 | Blur 0.8 | Noise 0.6 | 漂移平均 |
|---|---:|---:|---:|---:|---:|
| noise-weighted MLP | 96.15% | 94.92% | 91.99% | 82.59% | 89.83% |

该 head 同时通过 `clean >= 92.8%` 与 `noise >= 79.5%` gate。相对 Edge baseline，三种
漂移分别提升约 `+13.10 / +7.40 / +3.08 pp`，因此无需重新提取完整 noise-only 特征，
也暂时不切换 DINOv3。

进一步审计发现，已有 train cache 本身已经保存了 train split 上 `0.3–1.0` 随机强度的三类
漂移特征。正式训练应复用这一分布，并把固定 `1.0/0.8/0.6` 只留给 validation；重新用 6B
提取固定强度 train cache 既耗时，也会使训练分布贴近验证点。因此已停止尚未产生产物的固定强度
exporter，改由 `build_boxcars_teacher_cache_from_feature_cache.py` 在几秒内使用 selected MLP
生成正式 logits 和严格对齐的 type/severity 元数据。

现有缓存位于远端：

```text
outputs/internvit6b_boxcars_head_20260809/
```

## 本轮代码修正

### 1. 先用已有缓存修复 noise head

`module_edge_perception/retrain_boxcars_cloud_teacher_head_from_cache.py` 不运行 6B
backbone。它按原 DataLoader 布局恢复 clean/corrupt 配对，并按确定性 seed 重建每条样本的
漂移类型，比较：

- general linear；
- noise-weighted linear；
- clean + noise specialist linear；
- noise-weighted small MLP。

epoch 选择使用 train 内部确定性 calibration tracks，正式 validation 只用于候选准入。
只有 `clean >= 92.8%` 且 `noise >= 79.5%` 时才生成 `selected_head.pt`；否则流水线停止，
下一步才是重新提取完整 noise cache，而不是盲目蒸馏。

### 2. 正式 exporter 不再允许 prototype

`module_edge_perception/export_boxcars_cloud_teacher_cache.py` 现在强制传入
`--head-checkpoint`，以训练好的 linear/MLP head 产生 logits。cache 同时保存：

- track index；
- drift type；
- severity；
- teacher feature；
- trained-head logits。

对于本轮已有的随机强度 feature cache，正式流水线使用
`build_boxcars_teacher_cache_from_feature_cache.py` 直接构建相同格式的监督，避免重复运行 6B。

### 3. Adapter 训练改为严格对齐

`module_edge_perception/train_boxcars_cloud_teacher_adapter.py` 已调整为：

- 漂移输入 `clean_probability=0`，不再出现 clean student 对 drift teacher 的错位；
- 每个 batch 校验 sample index、drift type、severity；
- anchor 改为 `student(clean, adapter on)` 对齐 `baseline(clean, adapter off)`；
- 正式路径默认冻结 backbone、norm 和 head，只训练 AdaptFormer；norm/head 解冻仅保留为显式消融；
- clean、illumination、blur、noise 分开验证，不再只报告混合平均值；
- 增加 `--ce-weight`，可区分有标签实验上界与真实无标签 cloud refresh；
- 正式训练拒绝 prototype logits cache。

## 一次性运行顺序

代码同步到 a4 后，在 adjust worktree 执行：

```bash
screen -dmS cloud_teacher_pipeline bash -lc \
  'cd /root/autodl-tmp/EdgeCloudRuntime/worktrees/adjust && \
   bash run_cloud_teacher_pipeline.sh > logs/cloud_teacher_pipeline_20260809.log 2>&1'
```

`run_cloud_teacher_pipeline.sh` 按以下顺序执行：

1. 几分钟的缓存 head 消融；未过 clean/noise gate 会立即停止。
2. 使用 `selected_head.pt` 和已有随机强度 feature cache 构建正式 teacher logits/features。
3. 相同数据上训练 label-only control。
4. 训练 `CE + KD + feature + anchor` cloud hybrid。
5. 训练不依赖上传样本人工标签的 `KD + feature + anchor` cloud unlabeled。
6. 自动生成 `summary.json` 与 `summary.md`，计算 cloud hybrid 相对 label-only 的逐条件差异和最终 gate。

首轮决定性实验使用固定 seed，每个 epoch 随机抽取 256 batch（约 1,024 条代表性 train
tracks），训练 4 epoch，并对完整 649 条 validation 分别评估四种条件。三组实验使用完全相同
的抽样顺序和优化配置。这既显著缩短首轮时间，也更贴近 u=1 只上传少量代表性样本的设定。
首轮已经证明 cloud-unlabeled 主系统 gate 通过；是否追加全量训练不再由 hybrid 指标决定，
而由独立 test、重复 seed 和监督消融的结果决定。

## 判断门槛

### Teacher gate

- Clean 不低于 92.8%；
- Noise 不低于 79.5%；
- illumination / blur 的已有优势不能大幅丢失。

若缓存 head 全部失败，才提取每条训练样本的完整 noise 特征，预计约 50 分钟。完整 noise
训练仍失败时，InternViT 不承担 noise 监督；再考虑 DINOv3 H+/L 或 domain-specific ViT-L。

### Adapter gate

- 主系统 gate：cloud-unlabeled 相对未适配 Edge 的三漂移平均提升至少 1 pp；
- cloud-unlabeled 与有标签 label-only 上界的平均差距不超过 2 pp；
- clean 基本保持，且每一种漂移都不能明显低于 Edge；
- hybrid 相对 label-only 的额外收益单独报告，不作为无标签系统路线的否决条件。

若 teacher 本身通过但 cloud Adapter 不超过 label-only，问题在蒸馏 loss/representation transfer，
不应立刻换大模型。若 teacher 本身不通过，才说明需要换 teacher 或进行更强 task adaptation。

## 当前状态

一次性代表性样本流水线已经完成，结果如下：

| Model | Clean | Illumination | Blur | Noise | Mean drift |
|---|---:|---:|---:|---:|---:|
| Edge baseline | 93.84% | 81.82% | 84.59% | 79.51% | 81.97% |
| Cloud teacher | 96.15% | 94.92% | 91.99% | 82.59% | 89.83% |
| Label-only Adapter | 94.30% | 83.05% | 87.52% | 87.67% | 86.08% |
| Cloud hybrid Adapter | 94.30% | 83.36% | 86.75% | 86.90% | 85.67% |
| Cloud unlabeled Adapter | 94.61% | 83.05% | 86.13% | 86.44% | 85.21% |

两项结论必须分开：

1. **Hybrid additive test 未通过**：有真实标签 CE 时，加入当前 KD+feature 后平均比 label-only
   低 0.41 pp，因此不能宣称 cloud teacher 在有标签训练上进一步增益。
2. **Unlabeled refresh test 通过**：完全不使用上传样本真实标签时，cloud-unlabeled 相对未适配
   Edge 平均提升 3.24 pp，只比有标签 label-only 上界低 0.87 pp；clean 保持，三种漂移均提升。

第二项才对应真实 u=1 场景：云端视觉教师替代缺失的人工标签，为代表性漂移样本产生监督并将
结果压缩到约 1.2MB Adapter。因此当前证据支持 InternViT cloud teacher 路线，不需要切换
DINOv3，也不需要为了救 hybrid 指标盲目调参。后续正式报告应把 label-only 明确标成 supervised
upper bound，把 cloud-unlabeled 作为系统主结果，hybrid 作为负向/中性消融。

## 下一步做什么

### 已完成：快速独立 test check

固定开发阶段已经选定的 Edge baseline、`cloud_unlabeled/best.pth` 与 `selected_head.pt`，在
官方 BoxCars `test` split 上做了一个快速、可复核的独立检查。官方 test 有 12,322 条且严重类别
不均衡（Porsche 仅 3 条），因此为了控制 6B 单卡推理时间，固定 seed 从该 split 中按类别容量分层
抽取 256 条：每类至少一条、其余保持官方类别比例。没有按 test 结果重选 head、epoch、loss 或 checkpoint。

| Model | Clean | Illumination | Blur | Noise | Mean drift |
|---|---:|---:|---:|---:|---:|
| Edge baseline | 85.55% | 74.61% | 73.83% | 69.92% | 72.79% |
| Cloud unlabeled Adapter | **87.50%** | **76.17%** | **74.22%** | **77.73%** | **76.04%** |
| Cloud teacher | 92.97% | 91.41% | 91.02% | 80.86% | 87.76% |

结论：独立 test 子集复现了开发集方向。cloud-unlabeled 相对 Edge 的三漂移平均提升
**3.26 pp**，clean 也提升 **1.95 pp**；noise 提升 **7.81 pp**。对 Edge→Adapter 的 2,000 次
配对 bootstrap，noise 差异的 95% CI 为 **[+3.91, +11.72] pp**；illumination 和 blur 的小幅提升
在这 256 条小样本上区间跨 0，因此不能把各单项的小提升说成强统计结论。

评估耗时 288 秒。结果、精确样本索引和 3,072 条逐样本预测记录分别位于：

```text
outputs/internvit6b_cloud_teacher_quick_test_20260809/
local/results/internvit6b_cloud_teacher_quick_test_20260809/
```

这已经足以决定“不切回 VLM、不换 DINO、继续采用 cloud-unlabeled u=1 方案”。它不是全量
12,322 条 test 的最终统计报告；若后续需要论文级严格结论，再固定 checkpoint 后补跑全量即可，
当前不需要为此阻塞系统工作。

### 现在只保留一个务实任务

把已经验证的 `cloud_unlabeled/best.pth` 接入 `u=1` demo：漂移检测后上传代表性轨迹，云端刷新
Adapter，下发 1,216,745-byte 参数包，边缘加载后继续本地推理。记录上传样本数、刷新时间、加载时间
和云端不可用时的 `u=0` 回退即可。

暂不做多 seed、四档样本预算、全量 Adapter 训练、DINOv3 下载、InternViT LoRA 或 hybrid 调参；
它们是报告增强项，而不是当前方案成立或 demo 前进的前置条件。完整的证据范围与术语见
`docs/实验状态与证据边界_20260809.md`。
