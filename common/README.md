# common · 三模块共用代码

存放模块 A / B / C 共享的代码，避免重复定义、保证接口一致：

- **数据格式定义**：感知帧、关键帧 Squery、提示（Penv / ωt）、权重差量包等的统一数据结构
- **接口约定实现**：SubNet LUT 的读写与索引、`{v_t, k_t} → SubNet_id` 映射等
- **公共工具**：日志、配置加载、指标计量（TTFT / 端到端时延 / 内存）等

> ⚠️ 修改本目录下的数据结构或接口，须先在 `docs/接口契约_v0.md` 中达成一致（接口先行）。
